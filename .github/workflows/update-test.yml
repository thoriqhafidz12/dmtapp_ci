name: test

on:
  schedule:
    - cron: '0 0 * * *' #UTC
  push:
    branches:
      - 'master'

jobs:
  composer:
    uses: dmapp_ci/.github/workflows/update-composer.yml@master
    secrets:
      token: ${{ secrets.ACTION_TOKEN }}
    with:
      git-name: thoriqhafidz12
      git-email: 111201911729@mhs.dinus.ac.id
      composer-path: ./composer
      branch: master
